---
phase: 06-docker-service-integration-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_generation_e2e.py
autonomous: true
requirements:
  - TEST-GEN-01
  - TEST-GEN-02
  - TEST-GEN-03

must_haves:
  truths:
    - "Generation E2E tests verify actual file output to disk with correct naming pattern"
    - "Generation failure scenarios degrade gracefully with proper error messages"
    - "Docker profile switch flow is tested end-to-end (detect wrong profile -> confirm -> switch)"
    - "Model restore happens even when generation fails (try/finally verified)"
  artifacts:
    - path: "tests/unit/test_generation_e2e.py"
      provides: "Generation pipeline E2E failure scenarios and file output verification"
      min_lines: 100
  key_links:
    - from: "tests/unit/test_generation_e2e.py"
      to: "clod._handle_generation_intent"
      via: "direct function calls with mocked dependencies"
      pattern: "_handle_generation_intent"
    - from: "tests/unit/test_generation_e2e.py"
      to: "clod._save_generation_output"
      via: "file output verification with tmp_path"
      pattern: "_save_generation_output"
---

<objective>
Create E2E tests for the generation pipeline covering failure scenarios, actual file output verification, and Docker profile switch flow.

Purpose: Existing generation tests mock `_generate_image` return values but don't verify file writes. Failure scenarios (service down, VRAM timeout, compose failure) have no coverage. These are the second-highest priority gaps.
Output: One test file with E2E generation tests and failure scenario coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-docker-service-integration-testing/06-CONTEXT.md
@.planning/phases/06-docker-service-integration-testing/06-RESEARCH.md
@tests/conftest.py
@tests/unit/test_generation_repl.py
@tests/unit/test_generation.py
@tests/unit/test_generation_video.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generation E2E failure scenarios and file output tests</name>
  <files>tests/unit/test_generation_e2e.py</files>
  <action>
Create `tests/unit/test_generation_e2e.py` with E2E tests for generation failure scenarios and file output verification.

Use the established `@patch("clod.fn")` decorator stacking pattern from `test_generation_repl.py`. Reuse `mock_generation_state` fixture from conftest.py. Use `tmp_path` for file output verification. Use `FakeConsole` from conftest.py.

**File output verification tests:**
- `test_generation_image_saves_to_disk`: Mock `_generate_image` to return a real file path in tmp_path. Write fake PNG bytes (`b"\x89PNG\r\n\x1a\n" + b"\x00" * 50`) to that path before the mock returns. Call `_handle_generation_intent("image_gen", "a sunset", ...)` with `sd_output_dir` set to tmp_path. Verify file exists at expected path, verify file content matches fake PNG bytes.
- `test_generation_video_saves_to_disk`: Same pattern but for video_gen intent with fake MP4 bytes and `_generate_video` mock.

**Failure scenario tests (pick 4 most impactful per user decision):**
- `test_generation_service_unreachable`: Mock `_ensure_generation_service` to return False. Call `_handle_generation_intent`. Verify it returns early without calling `_craft_sd_prompt` or `_generate_image`. Verify `_silent_restore_model` is still called (try/finally).
- `test_generation_craft_prompt_failure`: Mock `_craft_sd_prompt` to raise Exception. Verify fallback to raw user input (per Phase 4 decision: "Craft failure falls back to raw user input rather than aborting"). Verify generation still proceeds with raw prompt.
- `test_generation_image_api_error`: Mock `_generate_image` to raise ConnectionError. Verify error is caught gracefully, `_silent_restore_model` called.
- `test_generation_vram_unload_failure`: Mock `_unload_model` to return False. Verify generation still proceeds (VRAM warning is non-blocking per Phase 1 decision).

**Docker profile switch E2E test:**
- `test_profile_switch_e2e_image_to_video`: Test the flow: user requests video_gen, but image profile is active. Mock `_ensure_generation_service` to simulate profile detection and switch. Verify the sequence: detect wrong profile -> user confirms -> stop current profile -> verify VRAM freed -> start new profile -> poll health. Use monkeypatch to control each step. Verify `_silent_restore_model` called at end.

**Model restore verification:**
- `test_model_restore_on_any_failure`: For each failure scenario above, assert `_silent_restore_model` is called exactly once, confirming try/finally behavior.

Follow the decorator stacking pattern from test_generation_repl.py:
```python
@patch("clod._silent_restore_model")
@patch("clod._generate_image", side_effect=ConnectionError("refused"))
@patch("clod._get_negative_prompts", return_value="bad")
@patch("clod._detect_sd_model_type", return_value="sd15")
@patch("clod._ensure_generation_service", return_value=True)
@patch("clod._unload_model", return_value=True)
@patch("clod._craft_sd_prompt", return_value=("prompt", "neg"))
@patch("clod._ensure_model_ready", return_value=True)
def test_generation_image_api_error(m_ensure, m_craft, ...):
```
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_generation_e2e.py -x -v --tb=short</automated>
  </verify>
  <done>7+ E2E generation tests covering: file output verification (image + video), 4 failure scenarios with graceful degradation, profile switch flow, and model restore guarantee on all paths.</done>
</task>

</tasks>

<verification>
python -m pytest tests/unit/test_generation_e2e.py -v --tb=short
</verification>

<success_criteria>
- test_generation_e2e.py has 7+ tests covering file output and failure scenarios
- All failure tests verify _silent_restore_model is called (try/finally guarantee)
- File output tests use tmp_path and verify actual file content
- All tests pass without Docker Desktop or real network calls
</success_criteria>

<output>
After completion, create `.planning/phases/06-docker-service-integration-testing/06-02-SUMMARY.md`
</output>
